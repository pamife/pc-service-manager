<UserControl x:Class="PcServiceManager.UI.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:PcServiceManager.UI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">
    <UserControl.Resources>
        <converters:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:NullToVisibilityConverter x:Key="NullToVis"/>
    </UserControl.Resources>

    <Grid>
        <!-- Main Service History View -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,24,28,32">
            <StackPanel MaxWidth="1200" HorizontalAlignment="Stretch">

                <!-- Header & Export Action -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Digital Service Book" FontSize="26" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBlock Text="Permanent service records and maintenance history for this PC." FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>

                    <ui:Button Grid.Column="1" Command="{Binding ExportCsvCommand}" Appearance="Secondary" Height="38" Padding="16,0">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Export History to CSV" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ui:Button>
                </Grid>

                <!-- Toast/Feedback message if any -->
                <Border Background="#2010B981" BorderBrush="#10B981" BorderThickness="1" CornerRadius="6" Padding="12,10" Margin="0,0,0,16" Visibility="{Binding NotificationMessage, Converter={StaticResource NullToVis}}">
                    <TextBlock Text="{Binding NotificationMessage}" FontSize="13" Foreground="#10B981"/>
                </Border>

                <!-- History Entries List -->
                <ItemsControl ItemsSource="{Binding ServiceSessions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                                    BorderThickness="1" CornerRadius="10" Padding="24,20" Margin="0,0,0,14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Date & Timestamp -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding StartTime, StringFormat='{}{0:dd MMMM yyyy}'}" FontSize="16" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding StartTime, StringFormat='{}{0:HH:mm} UTC'}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding DurationMinutes, StringFormat='{}Duration: {0} min'}" FontSize="12" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" Margin="0,4,0,0"/>
                                    </StackPanel>

                                    <!-- Service Details & Note -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="16,0">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="{Binding TemplateName}" FontSize="17" FontWeight="SemiBold"/>
                                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="10" Padding="8,2" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding PerformedBy, StringFormat='{}Serviced by {0}'}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding OverallNote}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>

                                    <!-- Metrics Badges -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <Border Background="#2010B981" CornerRadius="12" Padding="8,3" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding CompletedCount, StringFormat='{}✓ {0} Done'}" FontSize="11" FontWeight="SemiBold" Foreground="#10B981"/>
                                            </Border>
                                            <Border Background="#20EF4444" CornerRadius="12" Padding="8,3" Visibility="{Binding NeedsAttentionCount, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Text="{Binding NeedsAttentionCount, StringFormat='{}⚠ {0}'}" FontSize="11" FontWeight="SemiBold" Foreground="#EF4444"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding TotalTasks, StringFormat='{}{0} tasks checked'}" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>

                                    <!-- View Details Button -->
                                    <ui:Button Grid.Column="3" 
                                            Command="{Binding DataContext.OpenSessionDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" 
                                            Appearance="Secondary" 
                                            Height="36" 
                                            Padding="16,0" 
                                            VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="View Details" Margin="0,0,6,0"/>
                                            <ui:SymbolIcon Symbol="ChevronRight24" FontSize="12"/>
                                        </StackPanel>
                                    </ui:Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Empty State -->
                <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" CornerRadius="10" Padding="40" 
                        Visibility="{Binding ServiceSessions.Count, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                    <StackPanel HorizontalAlignment="Center">
                        <ui:SymbolIcon Symbol="BookOpen24" FontSize="40" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                        <TextBlock Text="No Service Records Yet" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="Your completed service sessions will appear here in chronological order, forming your PC's digital service book." FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- ========================================================================= -->
        <!-- DETAIL MODAL / DRAWER VIEW                                                -->
        <!-- ========================================================================= -->
        <Border Background="#90000000" Visibility="{Binding IsDetailViewOpen, Converter={StaticResource BoolToVis}}">
            <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="12" 
                    MaxWidth="900" 
                    MaxHeight="750" 
                    Margin="32" 
                    Padding="28">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding SelectedSession.TemplateName, StringFormat='{}Service Record: {0}'}" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding SelectedSession.StartTime, StringFormat='{}{0:dddd, MMMM dd, yyyy HH:mm}'}" FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <ui:Button Grid.Column="1" Command="{Binding CloseDetailsCommand}" Appearance="Transparent" Padding="8">
                            <ui:SymbolIcon Symbol="Dismiss24" FontSize="16"/>
                        </ui:Button>
                    </Grid>

                    <!-- Scrollable Task Results -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                        <StackPanel>
                            <!-- Summary Bar -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="DURATION" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding SelectedSession.DurationMinutes, StringFormat='{}{0} Minutes'}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                                    <StackPanel>
                                        <TextBlock Text="SERVICED BY" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding SelectedSession.PerformedBy}" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="6" Padding="12,8">
                                    <StackPanel>
                                        <TextBlock Text="RESULTS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        <TextBlock Text="{Binding SelectedSession.CompletedCount, StringFormat='{}{0} Completed'}" FontWeight="SemiBold" Foreground="#10B981"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Overall Note -->
                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Padding="14" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock Text="Technician Notes" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding SelectedSession.OverallNote}" FontSize="13" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>

                            <TextBlock Text="Checklist Outcomes" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10"/>

                            <!-- Tasks List -->
                            <ItemsControl ItemsSource="{Binding SelectedSession.TaskResults}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Border Grid.Column="0" 
                                                        Background="{Binding Status, Converter={StaticResource StatusToBrushConverter}}" 
                                                        CornerRadius="4" 
                                                        Padding="8,3" 
                                                        Margin="0,0,12,0" 
                                                        VerticalAlignment="Top">
                                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                                </Border>

                                                <StackPanel Grid.Column="1">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                        <TextBlock Text="{Binding TaskTitle}" FontSize="14" FontWeight="SemiBold"/>
                                                        <TextBlock Text="{Binding CategoryName, StringFormat='{} ({0})'}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                    </StackPanel>
                                                    <TextBlock Text="{Binding Notes}" FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Visibility="{Binding Notes, Converter={StaticResource NullToVis}}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer Actions -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Command="{Binding CopyReportTextCommand}" CommandParameter="{Binding SelectedSession}" Appearance="Secondary" Height="36" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Copy24" FontSize="14" Margin="0,0,6,0"/>
                                <TextBlock Text="Copy Service Report"/>
                            </StackPanel>
                        </ui:Button>
                        <ui:Button Content="Close" Command="{Binding CloseDetailsCommand}" Appearance="Primary" Height="36" Padding="20,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

    </Grid>
</UserControl>
