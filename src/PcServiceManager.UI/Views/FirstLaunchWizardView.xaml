<UserControl x:Class="PcServiceManager.UI.Views.FirstLaunchWizardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:enums="clr-namespace:PcServiceManager.Core.Enums;assembly=PcServiceManager.Core"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="{DynamicResource ApplicationBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
            <StackPanel MaxWidth="640" HorizontalAlignment="Center" VerticalAlignment="Center">

                <!-- Header Icon & Welcome -->
                <Border Background="{DynamicResource AccentFillColorDefaultBrush}" Width="60" Height="60" CornerRadius="30" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <ui:SymbolIcon Symbol="Desktop24" FontSize="30" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="Welcome to PC Service Manager" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                <TextBlock Text="Set up your PC's digital service book to track maintenance schedules, perform guided service sessions, and record history." 
                           FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,28"/>

                <!-- Setup Card -->
                <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" 
                        BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" 
                        BorderThickness="1" CornerRadius="12" Padding="28">
                    <StackPanel>

                        <!-- Hardware Detection Auto-Summary -->
                        <Border Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8" Padding="14,10" Margin="0,0,0,20">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <ui:SymbolIcon Symbol="CheckmarkCircle24" FontSize="14" Foreground="#10B981" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="DETECTED SYSTEM HARDWARE" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding DetectedHardwareSummary}" FontSize="12" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- PC Name Input -->
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="What should this PC be called?" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ui:TextBox Text="{Binding PcName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="e.g. Brother-PC, LivingRoom-Gaming, Office-PC" Height="38"/>
                        </StackPanel>

                        <!-- Device Form Factor -->
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="PC Form Factor" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ComboBox SelectedItem="{Binding DeviceType}" Height="38" HorizontalAlignment="Stretch">
                                <enums:DeviceType>Desktop</enums:DeviceType>
                                <enums:DeviceType>Laptop</enums:DeviceType>
                                <enums:DeviceType>AllInOne</enums:DeviceType>
                                <enums:DeviceType>Server</enums:DeviceType>
                                <enums:DeviceType>Other</enums:DeviceType>
                            </ComboBox>
                        </StackPanel>

                        <!-- Technician / Owner Name -->
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Primary Maintainer / Technician Name" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ui:TextBox Text="{Binding TechnicianName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Your Name (e.g. Paul)" Height="38"/>
                        </StackPanel>

                        <!-- Initial Notes -->
                        <StackPanel Margin="0,0,0,24">
                            <TextBlock Text="PC Notes (Optional)" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Height="50" AcceptsReturn="True" TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- Submit Button -->
                        <ui:Button Command="{Binding CompleteSetupCommand}" Appearance="Primary" Height="48" HorizontalAlignment="Stretch" FontSize="15" FontWeight="SemiBold">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="Checkmark24" FontSize="18" Margin="0,0,8,0"/>
                                <TextBlock Text="Create Digital Service Book &amp; Start" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ui:Button>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
